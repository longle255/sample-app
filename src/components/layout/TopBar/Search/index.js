import React from 'react'
import styles from './style.module.scss'

class Search extends React.Component {
  render() {
    return <i className={`${styles.icon} fe fe-search`} />
  }
}

export default Search
